# Runtime Assets

Runtime assets live here when implementation needs bundled imports. Spine can be used as the animation source, but the game runtime should only load exported sequence frames, spritesheets, or Phaser texture atlases.
